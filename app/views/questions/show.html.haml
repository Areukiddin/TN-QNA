%h2= @question.title
%h3= @question.body

%h4= "Answers:"

= render @question.answers

= form_with model: [@question, @question.answers.new], local: true do |f|
  = f.label :body
  = f.text_field :body

  = f.submit 'Answer'
