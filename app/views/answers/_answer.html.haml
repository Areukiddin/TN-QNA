%p.answer= answer.body
- if user_signed_in? && current_user.author_of?(answer)
  %p.delete= link_to 'Delete answer', answer_path(answer), method: :delete, data: { confirm: 'Are you sure to delete answer?' }
